import java.util.Scanner;

public class DemoJava {
    public static void main(String[] args) {


        /*
      for (int i = 0; i < 10; i++) { // 2 < 10 (true)
          if (i % 2 == 0) {
              continue;
          }
          System.out.println(i);// 1
      }
        // break: Khi gặp break dừng vòng lặp gần nhất
        // continue: Khi gặp continue, bỏ qua vòng lặp hiện tại(các đoạn code bên dưới và chuyển tới vòng lặp tiếp theo theo)

        int i = 10;
        do {
            System.out.println(i);
            i--;
        }while(i > 0);
    }
    // do while dù đúng hay sai thì cũng thực thi ít nhát 1 lần
    // Cho người dùng nhập số vvaf dừng lại khi số đó bằng 0 (dùng do while)


    Scanner sc = new Scanner(System.in);
    int value;
    do {
        System.out.println("mời nhập số");
        value = sc.nextInt();
    }while (value != 0);
         */
    /*
    while(<điều kiện dừng>){
        <câu lệnh thực thi>
    }
    nếu <điều kiện dừng> trả về false thì sẽ lập tức dừng vòng lặp
     int i =0;
     while(i < 10) {
         System.out.println(i);
         i++;
     }
     */



    }
}
